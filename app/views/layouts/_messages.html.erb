<div class="row">
    <% flash.each do |name, msg| %>
        <div class="ui message container <%="#{name}"%>">
            <i class="close icon"></i>
            <%= content_tag :p, msg, :id => "flash_#{name}" if msg.is_a?(String) %>
        </div>
    <% end %>
</div> 